google anthos?

[x]ephemeral bootstrap server -- GITPOD!!

?url to git repo for target app (RO)
?url to git repo for cluster state (RW?)

automate:
[x] creation of google project
[x] standup of GKE cluster
[] install of argoCD
[] setup of argoCD to a repo(s)
[x] teardown of all the above

build:
[] lab repo for argoCD 
[] 'prod' repo for argoCD (future)
[] infra repo (prom, graf, etc?) with prod and lab branch
[] example app repo, with prod and lab branch
[] submodule example app integration branch into lab argoCD repo
